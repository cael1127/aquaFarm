name: aquafarm-ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  backend-smoke:
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis:7-alpine
        ports: ["6379:6379"]
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 20
      timescaledb:
        image: timescale/timescaledb:latest-pg16
        env:
          POSTGRES_USER: aquafarm
          POSTGRES_PASSWORD: aquafarm
          POSTGRES_DB: aquafarm
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U aquafarm -d aquafarm"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    defaults:
      run:
        working-directory: backend
    env:
      DATABASE_URL: postgresql+asyncpg://aquafarm:aquafarm@localhost:5432/aquafarm
      REDIS_URL: redis://localhost:6379/0
      DEVICE_TOKEN: dev-sensor-token
      CORS_ORIGIN: http://localhost:3001
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install deps
        run: pip install -r requirements.txt
      - name: Init schema
        run: |
          sudo apt-get update && sudo apt-get install -y postgresql-client
          PGPASSWORD=aquafarm psql -h localhost -U aquafarm -d aquafarm -f sql/init.sql
          PGPASSWORD=aquafarm psql -h localhost -U aquafarm -d aquafarm -f sql/migrate.sql
      - name: Start API
        run: |
          set -e
          uvicorn app.main:app --host 127.0.0.1 --port 8000 > /tmp/aquafarm-api.log 2>&1 &
          echo $! > /tmp/aquafarm-api.pid
          for i in $(seq 1 40); do
            if curl -sf http://127.0.0.1:8000/health; then
              echo "api healthy"
              exit 0
            fi
            sleep 1
          done
          echo "=== api log ==="
          cat /tmp/aquafarm-api.log || true
          exit 1
      - name: Start worker
        run: |
          python -m app.worker > /tmp/aquafarm-worker.log 2>&1 &
          sleep 2
      - name: Smoke ingest
        working-directory: .
        run: python scripts/smoke_test.py
        env:
          API_URL: http://127.0.0.1:8000
          DEVICE_TOKEN: dev-sensor-token
          LATENCY_BUDGET_S: "8"

  web-typecheck:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm install
      - run: npm run typecheck
